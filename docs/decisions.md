# 결정 기록

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-08-20 | 학습 프레임워크 TRL + PEFT | SFT→GRPO 를 한 스택으로. LLaMA-Factory/Unsloth 대비 RL 전환 비용 최소 |
| 2026-08-20 | 교사 모델은 `claude`/`codex` CLI 서브프로세스 호출 | 별도 API 키 관리 불필요, 이미 로그인된 세션 재사용 |
| 2026-08-20 | 대용량 산출물은 레포 내 `data/ models/ outputs/` (gitignore), 공유 디렉토리 없음 | 지금 단계엔 불필요. 공유 필요 시 HF Hub private repo |
| 2026-08-20 | Python 3.12 | 3.13 은 vLLM/flash-attn 휠이 아직 불안정 |
| 2026-08-20 | torch 는 cu128 휠 고정 | PyPI 기본 cu130 은 드라이버 ≥580 필요. cu128 은 ≥570 에서 동작해 호환 범위가 넓다. 클라우드 이미지 드라이버 확인 후 조정 |
| 2026-08-21 | 학습은 클라우드 GPU, 로컬은 데이터 작업만 → 학습 스택은 `[train]` extra 로 분리 | 데이터 작업자가 torch 를 받을 이유가 없다 |
| 2026-08-20 | 베이스 모델 미확정 (configs/sft 의 `model_name` 한 줄) | 학습 장비 확정 후 실험으로 결정 |
| 2026-08-20 | 데이터 입력 형태 미확정 (시나리오 스펙 기반 합성 vs 실관측 데이터) | 실험으로 결정 |
| 2026-08-21 | 초기 레포는 규칙과 디렉토리 골격만. 로직은 넣지 않는다 | 아직 방식이 정해지지 않았다. 추측으로 넣은 코드가 "정해진 방식"으로 읽히는 것을 막기 위해 |
| 2026-08-21 | 학습 모니터링은 W&B, 팀 프로젝트 `rca-model-lab` 하나에 전부 | 여러 명이 클라우드에서 따로 돌리는 run 을 한 곳에서 비교하기 위해. SFT 와 GRPO 를 같은 프로젝트에 둔다 |
| 2026-08-21 | 브랜치 전략은 trunk-based (main + 단명 feature 브랜치, squash). 실험은 브랜치가 아니라 config+W&B | 배포 없는 2~3인 실험 레포. 실험마다 브랜치를 따면 코드가 갈라져 결과 비교가 안 된다 |
| 2026-08-21 | 데이터셋·가중치 공유는 HF Hub private (org `nkia-ai-lab`), 버전은 태그 | 클라우드 GPU 에서 `load_dataset(revision=)` 한 줄로 재현 가능. git LFS 는 레포를 무겁게 하고 데이터 버전을 코드 버전에 묶는다. 외부 업로드가 정책상 막히면 사내 스토리지+DVC 로 전환 |
